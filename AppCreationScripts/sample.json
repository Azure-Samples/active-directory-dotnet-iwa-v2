{
  "Sample": {
    "Title": ".NET Console application using MSAL.NET with Integrated Windows Authentication to acquire an access token and call Microsoft Graph API",
    "Level": 200,
    "Client": "Desktop (Console)",
    "Service": "Microsoft Graph",
    "RepositoryUrl": "active-directory-dotnet-iwa-v2",
    "Endpoint": "AAD v2.0"
  },

  /*
    This section describes the Azure AD Applications to configure, and their dependencies
  */
  "AADApps": [
    {
      "Id": "client",
      "Name": "iwa-console",
      "Kind": "Desktop",
      "UsesROPCOrIWA": true,
      "Audience": "AzureADMultipleOrgs",
      "RequiredResourcesAccess": [
        {
          "Resource": "Microsoft Graph",
          "DelegatedPermissions": [ "User.Read", "User.ReadBasic.All" ]
        }
      ]
    }
  ],

  /*
    This section describes how to update the code in configuration files from the apps coordinates, once the apps
    are created in Azure AD.
    Each section describes a configuration file, for one of the apps, it's type (XML, JSon, plain text), its location
    with respect to the root of the sample, and the mappping (which string in the config file is mapped to which value
  */
  "CodeConfiguration": [
    {
      "App": "client",
      "SettingKind": "JSon",
      "SettingFile": "\\..\\iwa-console\\appsettings.json",
      "Mappings": [
        {
          "key": "ClientId",
          "value": ".AppId"
        }
      ]
    }
  ]
}
